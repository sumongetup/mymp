# Copy to .env.local for local work. On Vercel, set these under Settings → Environment Variables.
# The public site needs NONE of these; they only power /admin, the build-time merge and the nightly rebuild.

# Canonical origin. Set once the real domain is attached.
NEXT_PUBLIC_SITE_URL=https://mymp.bd

# Supabase → Project Settings → API. Use a project dedicated to this site.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Secret. Used only inside server actions and the build-time sync.
SUPABASE_SERVICE_ROLE_KEY=

# The first super admin. Create this user in Supabase → Authentication → Users first,
# then sign in at /admin/login; the row is created on that first sign-in.
ADMIN_BOOTSTRAP_EMAIL=

# Vercel → Settings → Git → Deploy Hooks → create one for branch main.
# "Publish" in the admin and the nightly cron both call it.
VERCEL_DEPLOY_HOOK_URL=

# Any long random string. Vercel Cron sends it as a bearer token to /api/cron/republish.
CRON_SECRET=
